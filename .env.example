# Supabase —— 配置后启用「登录 + 云端数据」；不配则本地走文件、免登录。
#
# 本地开发：复制本文件为 .env.local 并填入真实值（.env.local 已被 gitignore）。
# 线上部署：在 Vercel 项目 Settings → Environment Variables 配同名变量。
#
# 两个值都在 Supabase 控制台 Project Settings → API 里。anon key 是设计上可公开的前端密钥，
# 真正的数据隔离由数据库 RLS 行级策略保证。

VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY
