# 服务端环境变量（自建后端）—— 均为服务端变量，无 VITE_ 前缀，不会进入前端产物。
#
# 本地全栈联调（vercel dev）：复制本文件为 .env.local 并填入真实值（.env.local 已 gitignore）。
# 线上部署：在 Vercel 项目 Settings → Environment Variables 配同名变量。
#
# 注：仅 `pnpm dev`（纯前端 + 文件存储 + 免登录）无需任何下列变量。

# Postgres 连接串。在 Vercel 面板从 Marketplace 开通 Neon 后会自动注入；
# 本地联调时填 Neon 控制台给出的连接串（含 ?sslmode=require）。
DATABASE_URL=postgres://USER:PASSWORD@HOST/DB?sslmode=require

# 会话签名密钥。随机长字符串，例如：openssl rand -base64 32
SESSION_SECRET=

# 注册邀请码。设置后仅持此码可创建账号；不设则禁止一切注册。
REGISTER_SECRET=
